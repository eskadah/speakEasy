<div class="navbar-wrapper">
  <div class="navbar navbar-default " id="navigate" role="navigation">
    <div class="container">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
         <% if current_user %>
            <a style = 'margin-top: -30px' class="navbar-brand"  id = 'logo' href=<%= user_path(current_user)%> data-no-turbolink= true ><h1>Speak Easy</h1></a>
         <% else %>
            <a style = 'margin-top: -30px' class="navbar-brand"  id = 'logo' href="/"><h1>Speak Easy</h1></a>
         <% end %>
      </div>
      <div class="navbar-collapse collapse">
        <% if current_user %>
            <ul class="nav navbar-nav pull-right">
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name.titleize%> <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <li><%= link_to('My Events', current_user, data:{:no_turbolink =>true})%></li>
                  <li><%= link_to('My Calendar', events_path, data:{:no_turbolink =>true})%></li>
                  <li><%= link_to 'Add New Event', new_event_path, data:{:no_turbolink =>true}%></li>
                  <li class="divider"></li>
                  <li><%= link_to('Edit Profile', edit_user_path(current_user))%></li>
                  <li><%= link_to('Log Out', sessions_destroy_path)%></li>
                </ul>

            </ul>
        <% else %>
            <ul class="nav navbar-nav pull-right">
              <li style = 'margin:10px'><%= render '/sessions/form' %></li>
          </ul>
        <% end %>
      </div>
    </div>
  </div>
</div>
